# xgboost-bridge

Export trained XGBoost models as portable JSON artifacts, with a reference predictor for validating them.

Companion to the [`xgboost-predictor`](https://www.npmjs.com/package/xgboost-predictor) npm package, which runs inference on those artifacts with zero dependencies in browser and edge runtimes.

Documentation is in progress.
